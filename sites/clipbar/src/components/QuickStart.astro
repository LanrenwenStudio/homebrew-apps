---
---
<section class="section" id="quickstart">
  <div class="shell">
    <div class="section-header">
      <div class="section-eyebrow">快速上手</div>
      <h2 class="section-title">三步完成配置</h2>
      <p class="section-subtitle">只需确保本机 CLIProxyAPI 已启动并开启管理接口密钥即可。</p>
    </div>

    <div class="steps-grid">
      <div class="step-card">
        <div class="step-number">1</div>
        <h3>1. 本机运行 CLIProxyAPI</h3>
        <p>本机启动 CLIProxyAPI（默认 <code>http://127.0.0.1:8317</code>），并在配置文件中设置 <code>remote-management.secret-key</code>。</p>
      </div>

      <div class="step-card">
        <div class="step-number">2</div>
        <h3>2. 填入管理地址与密钥</h3>
        <p>打开 ClipBar，右键状态栏图标打开设置，填入管理地址和刚才设置的 <code>secret-key</code>。</p>
      </div>

      <div class="step-card">
        <div class="step-number">3</div>
        <h3>3. 状态栏查看额度走势</h3>
        <p>菜单栏将实时显示剩余额度百分比，左键点击即可展开查看每个订阅账号的额度进度条与重置倒计时。</p>
      </div>
    </div>
  </div>
</section>
